name: Checks

on:
  pull_request:
    branches:
    - master
    - 3.8
    - 3.7

jobs:
  whitespace:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v1
    - name: Patchcheck
      run: SRCDIR=$(pwd) python3 Tools/scripts/patchcheck.py --base-branch origin/${{github.base_ref}}
